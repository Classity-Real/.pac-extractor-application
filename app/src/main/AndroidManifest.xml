<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No broad storage permissions needed: we use SAF (ACTION_OPEN_DOCUMENT /
         ACTION_OPEN_DOCUMENT_TREE), which grants scoped access without them. -->

    <!-- Required on API 33+ to post the foreground-service notification from
         ExtractionService. Without it granted, the service still runs in the
         foreground fine — the notification just silently doesn't show — but the
         manifest declaration is what makes that runtime permission requestable
         at all, and is what lint's NotificationPermission check requires. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PExt">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PExt.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.ExtractionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
